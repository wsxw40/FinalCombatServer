<?xml version="1.0" encoding="utf-8"?>
<mx:TitleWindow xmlns:mx="http://www.adobe.com/2006/mxml" layout="absolute" width="400" height="300"
	label="Point" title="Point"
	showCloseButton="true" close="{PopUpManager.removePopUp(this)}">
	<mx:Script>
		<![CDATA[
			import vo.PointVO;
			import mx.controls.Alert;
			import vo.GamePoint;
			import mx.collections.ArrayCollection;
			import mx.managers.PopUpManager;
			[Bindable]
			public  var points:ArrayCollection=new ArrayCollection();
			[Bindable]
			public  var point:PointVO;
			private function onNewBtnClick():void{
				var point:PointVO=new PointVO();
				points.addItem(point);
			}
			private function onDeleteBtnClick():void{
				if(this.point==null){
					Alert.show("请选择一条记录。");
				}else{
					var index:int=pointDatagrid.selectedIndex;
					points.removeItemAt(index);
				}
			}
			
			private function onCancelBtnClick():void{
				PopUpManager.removePopUp(this);
			}
			private function selectHandler():void{
				this.point=pointDatagrid.selectedItem as PointVO;
			}
			
		]]>
	</mx:Script>
<mx:VBox>
	<mx:VBox>
		<mx:DataGrid id="pointDatagrid" dataProvider="{points}" editable="true" width="378" height="194" itemClick="selectHandler()">
		</mx:DataGrid>
	</mx:VBox>
	<mx:VBox>
		<mx:ControlBar width="100%" >
				<mx:Button id="save"  label="保存"/>		
	        	<mx:Button id="newBtn"  label="新建" click="onNewBtnClick()"/>
	        	<mx:Button id="deleteBtn" label="删除" click="onDeleteBtnClick()"/>
	        	<mx:Button id="cancelBtn"  label="退出"  click="onCancelBtnClick()"/>
	        
	     </mx:ControlBar>
	</mx:VBox>
</mx:VBox>
</mx:TitleWindow>
